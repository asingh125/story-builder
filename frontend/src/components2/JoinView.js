import React, { useState } from "react";
import {Button, Paper} from '@material-ui/core';

const JoinView = props => {
    return (
      <div id="JoinView">
        <Typography>This is the Join Page!</Typography>
        <Button>Join game</Button>
      </div>
    )
}

export default JoinView